# Agent Configuration Standard

本仓库维护可复用的 Agent 配置规范。目标是让 Agent 根据目标项目的实际情况增量接入能力。

## 入口路由

- 输入：用户任务、目标项目位置及本仓库内容。
- 处理：先区分用户是在维护本规范，还是要求将规范集成到另一个项目。
- 判断：目标明确且要求集成时，读取 [CONFIG.md](CONFIG.md) 并依序执行；目标不明确时，先完成规范阅读与只读调查，再询问目标位置。要求维护规范时，仅修改被授权的规范内容。
- 输出：集成任务输出 CONFIG 中的四项报告；维护任务输出修改内容及验证结果。

## 维护约束

1. 本仓库提供规范、工作流、Skill、子 Agent 定义和 Memory 结构，不承担运行时、MCP 或 Memory 服务职责。
2. `template/` 是能力来源。不得将其不经分析直接复制覆盖目标项目，也不得把模板中的行为误当成本规范仓库已经安装的运行配置。
3. 模板中不得包含真实项目数据、业务经验、历史 lessons、用户个人偏好、凭据或本机路径。示例必须明确标为虚构，放入 `examples/`。
4. 每项流程必须包含输入、处理、判断条件和输出。规则应能指导下一步操作，避免空泛口号。
5. Skill 是审查契约来源；Memory structure 定义存储，lifecycle 定义状态，index 定义索引，retrieval 定义任务加载，Memory Manager 定义治理决策。其他文件引用这些单一来源，修改时同步检查引用方。
6. 保留目标项目已有规则。发现实质冲突时报告冲突及影响；不得静默覆盖，也不得把低优先级模板提升为宿主系统指令。
7. 能否自动读取 AGENTS、发现 Skill 或调用子 Agent，取决于宿主。根据实际配置验证，不宣称通用自动加载。
8. [VERSION](VERSION) 是发布版本来源；配置版本与宿主兼容记录遵循 CONFIG。升级不自动激活历史经验，任务只经 Index 加载匹配的 Active Memory。

## 变更验收

- 输入：本次修改的文档和差异。
- 处理：检查必需文件、相对链接、输入输出字段、判断分支、生命周期、模板数据边界；用集成示例检查可操作性和重复执行行为。
- 判断：发现字段、状态、路径或职责冲突时先修复；无法实际验证的宿主行为注明未验证。
- 输出：文件职责、设计说明、使用方式、验证结果和剩余限制。
